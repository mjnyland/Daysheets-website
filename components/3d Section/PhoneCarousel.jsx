// PhoneCarousel.jsx
"use client";

import { Suspense, useLayoutEffect, useRef } from "react";
import { Model } from "./DayViewModel"; // <-- import your DayViewModel.jsx

export function PhoneCarousel({ onReady }) {
  const groupRef = useRef(null);

  return <Model />;
}
